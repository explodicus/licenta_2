<p><%= t('Welcome') + ' ' + @resource.full_name %>!</p>

<p><%= t('You can confirm your account email through the link below') %>:</p>

<p><%= link_to t('Confirm my account'), confirmation_url(@resource, confirmation_token: @token, locale: I18n.locale) %></p>

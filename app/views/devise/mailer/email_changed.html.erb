<p><%= t('Hello') + @resource.full_name %>!</p>

<% if @resource.try(:unconfirmed_email?) %>
  <p><%=t("We're contacting you to notify you that your email is being changed to") + ' ' + @resource.unconfirmed_email %>.</p>
<% else %>
  <p><%= t("We're contacting you to notify you that your email has been changed to") + ' ' + @resource.email %>.</p>
<% end %>
